# Development environment settings  
NODE_ENV=production

# Application URL for production deployment
APP_URL=https://amphoreus.replit.app

# Security settings
ENABLE_GEOBLOCKING=true
VERBOSE_LOGGING=true

# Database settings
# Note: DATABASE_URL is automatically provided by Replit
DB_MAX_CONNECTIONS=10